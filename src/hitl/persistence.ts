// Re-exports DB types for convenience; actual persistence is in AuditLogger/AuditDb.
// This module documents the persistence contract and handles startup recovery.

export { HitlEngine } from './engine.js';
